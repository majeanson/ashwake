# SWEEP.md — what the rituals found, run by a program

Generated by `pnpm sweep` on 2026-09-10. **Do not edit by hand.**

Walked **273 files** in 82.6s, over 2 of 6 passes. **321 findings**, 0 absorbed by `scripts/sweep/allow.ts` (0 rulings).

## module — 251

| grade | where | what |
| --- | --- | --- |
| certain | `apps/game/src/board/Board.tsx:105` | `Aim` is exported but read only inside its own file (4) |
| certain | `apps/game/src/board/Board.tsx:202` | `BoardProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/Board.tsx:264` | `LeanSet` is exported but read only inside its own file (6) |
| certain | `apps/game/src/board/HexField.tsx:63` | `HexFieldProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/Labels.tsx:57` | `LabelsProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/LightRig.tsx:24` | `LightRigProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/Pop.tsx:41` | `PopProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/ambient.ts:13` | `BREATH_MS` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/ambient.ts:61` | `EmberPhase` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/camera.ts:36` | `HEX_PX_MAX` is exported but read only inside its own file (3) |
| certain | `apps/game/src/board/camera.ts:103` | `FLAT` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/camera.ts:260` | `pxPerUnit` is exported but read only inside its own file (2) |
| certain | `apps/game/src/board/camera.ts:338` | `FIT_HEX_PX_MIN` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/camera.ts:367` | `Eye` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/camera.ts:430` | `GLIDE_FLOOR` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/camera.ts:497` | `TILT_MIN` is exported but read only inside its own file (2) |
| certain | `apps/game/src/board/camera.ts:498` | `TILT_MAX` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/camera.ts:530` | `TwoFinger` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/ground.ts:72` | `gutterOf` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/ground.ts:85` | `GroundItem` is exported but read only inside its own file (3) |
| certain | `apps/game/src/board/ground.ts:105` | `GroundOpts` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/keys.ts:47` | `TURN_STEP` is exported but read only inside its own file (2) |
| certain | `apps/game/src/board/keys.ts:48` | `LEAN_STEP` is exported but read only inside its own file (2) |
| certain | `apps/game/src/board/keys.ts:57` | `BoardCommand` is exported but read only inside its own file (2) |
| certain | `apps/game/src/board/keys.ts:70` | `Chord` is exported but read only inside its own file (3) |
| certain | `apps/game/src/board/keys.ts:144` | `FocusKind` is exported but read only inside its own file (2) |
| certain | `apps/game/src/board/leap.ts:29` | `LEAP_MS` is exported but read only inside its own file (2) |
| certain | `apps/game/src/board/leap.ts:78` | `LeapPhase` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/leap.ts:115` | `GlowPhase` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/relief.ts:134` | `jitterAt` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/resources.ts:30` | `BatchResources` is exported but read only inside its own file (1) |
| certain | `apps/game/src/board/rings.ts:18` | `Ring` is exported but read only inside its own file (2) |
| certain | `apps/game/src/board/rings.ts:23` | `PlacedRing` is exported but read only inside its own file (2) |
| certain | `apps/game/src/board/rings.ts:33` | `ringOf` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/ActionBar.tsx:41` | `ActionBarProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/Camera.tsx:69` | `View` is exported but read only inside its own file (3) |
| certain | `apps/game/src/screens/Camera.tsx:71` | `CameraProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/Device.tsx:38` | `DeviceProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/Directions.tsx:31` | `DirectionsProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/EndScreen.tsx:39` | `EndScreenProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/Fame.tsx:35` | `FameProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/FrontDoor.tsx:34` | `FrontDoorProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/Hud.tsx:21` | `HudProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/Hud.tsx:30` | `StatId` is exported but read only inside its own file (3) |
| certain | `apps/game/src/screens/Legend.tsx:72` | `LegendProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/LessonCard.tsx:31` | `LessonCardProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/Manual.tsx:165` | `ManualProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/More.tsx:49` | `MoreProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/Payout.tsx:27` | `PayoutProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/Playtest.tsx:48` | `PlaytestProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/Purse.tsx:54` | `PurseProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/SaidCard.tsx:39` | `SaidCardProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/Settings.tsx:57` | `SettingsProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/Shop.tsx:39` | `ShopProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/screens/Worlds.tsx:27` | `WorldsProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/beginning.ts:64` | `Door` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/beginning.ts:117` | `Wiring` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/cross.ts:62` | `Crossing` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/cross.ts:74` | `Crossed` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/dry.ts:33` | `Dryness` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/economy.ts:36` | `RunKind` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/failure.ts:35` | `describeError` is exported but read only inside its own file (2) |
| certain | `apps/game/src/shell/fixture.ts:87` | `FIXTURE_SEED` is exported but read only inside its own file (2) |
| certain | `apps/game/src/shell/fixture.ts:146` | `worldAged` is exported but read only inside its own file (2) |
| certain | `apps/game/src/shell/fixture.ts:217` | `purseAfter` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/ledgers.ts:31` | `Ledgers` is exported but read only inside its own file (3) |
| certain | `apps/game/src/shell/onceARun.ts:26` | `Moment` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/playtest.ts:69` | `Note` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/settle.ts:37` | `Settled` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/settle.ts:103` | `Settling` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/settle.ts:304` | `SettledDaily` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/settle.ts:321` | `SettlingDaily` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/share.ts:118` | `HandOffResult` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/shareCard.ts:31` | `ShareCardData` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/signpost.ts:32` | `Signpost` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/storage.ts:458` | `LastError` is exported but read only inside its own file (2) |
| certain | `apps/game/src/shell/storage.ts:505` | `SaidOnce` is exported but read only inside its own file (2) |
| certain | `apps/game/src/shell/storage.ts:651` | `NO_MEMORY` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/storage.ts:760` | `isFreeSlot` is exported and nothing reads it, anywhere |
| certain | `apps/game/src/shell/teaching.ts:32` | `Teach` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/teaching.ts:85` | `Moment` is exported but read only inside its own file (3) |
| certain | `apps/game/src/shell/touch.ts:58` | `Buzz` is exported but read only inside its own file (1) |
| certain | `apps/game/src/shell/useDevice.ts:44` | `Device` is exported but read only inside its own file (1) |
| certain | `apps/game/src/ui/Bars.tsx:21` | `Bar` is exported but read only inside its own file (1) |
| certain | `apps/game/src/ui/Card.tsx:51` | `CardProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/ui/Confirming.tsx:18` | `ConfirmingProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/ui/FactGrid.tsx:10` | `Fact` is exported but read only inside its own file (1) |
| certain | `apps/game/src/ui/Figure.tsx:43` | `FigureProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/ui/Hex.tsx:46` | `HexProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/ui/Icon.tsx:32` | `IconProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/ui/Panel.tsx:19` | `PanelProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/ui/Prose.tsx:28` | `ProseProps` is exported but read only inside its own file (2) |
| certain | `apps/game/src/ui/Tabs.tsx:33` | `Tab` is exported but read only inside its own file (1) |
| certain | `apps/game/src/ui/Tabs.tsx:48` | `TabsProps` is exported but read only inside its own file (1) |
| certain | `apps/game/src/ui/Tile.tsx:74` | `TileProps` is exported but read only inside its own file (1) |
| certain | `packages/core/src/content/goals.ts:28` | `Goal` is exported but read only inside its own file (1) |
| certain | `packages/core/src/content/locale.ts:16` | `DEFAULT_LOCALE` is exported but read only inside its own file (1) |
| certain | `packages/core/src/content/tuning.ts:692` | `BARE_TUNING` is exported but read only inside its own file (1) |
| certain | `packages/core/src/engine/hex.ts:15` | `keyOf` is read only by tests (13) |
| certain | `packages/core/src/engine/hex.ts:30` | `DIRECTIONS` is exported but read only inside its own file (2) |
| certain | `packages/core/src/engine/hex.ts:39` | `neighbours` is read only by tests (5) |
| certain | `packages/core/src/engine/hex.ts:49` | `disc` is read only by tests (72) |
| certain | `packages/core/src/engine/hex.ts:63` | `toPixel` is read only by tests (6) |
| certain | `packages/core/src/engine/rng.ts:47` | `rngChance` is read only by tests (4) |
| certain | `packages/core/src/engine/rules.ts:15` | `Cells` is exported but read only inside its own file (14) |
| certain | `packages/core/src/engine/rules.ts:42` | `isSolid` is exported but read only inside its own file (1) |
| certain | `packages/core/src/engine/rules.ts:142` | `ripeClusterAt` is exported but read only inside its own file (2) |
| certain | `packages/core/src/engine/rules.ts:336` | `rootboundGrip` is exported but read only inside its own file (1) |
| certain | `packages/core/src/engine/rules.ts:500` | `questMet` is exported but read only inside its own file (2) |
| certain | `packages/core/src/engine/rules.ts:625` | `worthParts` is exported but read only inside its own file (1) |
| certain | `packages/core/src/engine/rules.ts:831` | `treasureFor` is exported but read only inside its own file (1) |
| certain | `packages/core/src/engine/rules.ts:874` | `blankMap` is read only by tests (8) |
| certain | `packages/core/src/engine/state.ts:82` | `Phase` is exported but read only inside its own file (1) |
| certain | `packages/core/src/engine/state.ts:152` | `HarvestRecord` is exported but read only inside its own file (1) |
| certain | `packages/core/src/engine/world.ts:18` | `Terrain` is exported but read only inside its own file (2) |
| certain | `packages/core/src/engine/world.ts:38` | `Destination` is exported but read only inside its own file (6) |
| certain | `packages/core/src/engine/world.ts:60` | `blockDestination` is exported but read only inside its own file (2) |
| certain | `packages/core/src/engine/world.ts:223` | `Find` is exported but read only inside its own file (4) |
| certain | `packages/core/src/engine/world.ts:236` | `blockFind` is exported but read only inside its own file (2) |
| certain | `packages/core/src/engine/world.ts:296` | `biomeAt` is exported but read only inside its own file (1) |
| certain | `packages/core/src/engine/world.ts:344` | `elevationAt` is exported but read only inside its own file (1) |
| certain | `packages/core/src/meta/backup.ts:103` | `migrateLegacy` is exported but read only inside its own file (1) |
| certain | `packages/core/src/meta/daily.ts:23` | `DAILY_EPOCH` is exported but read only inside its own file (1) |
| certain | `packages/core/src/meta/daily.ts:32` | `DAILY_FIRST` is exported but read only inside its own file (1) |
| certain | `packages/core/src/meta/daily.ts:35` | `isDailyDate` is exported but read only inside its own file (3) |
| certain | `packages/core/src/meta/daily.ts:78` | `previousDate` is exported but read only inside its own file (2) |
| certain | `packages/core/src/meta/daily.ts:83` | `dailyNumber` is exported but read only inside its own file (1) |
| certain | `packages/core/src/meta/daily.ts:111` | `DailyRecord` is exported but read only inside its own file (4) |
| certain | `packages/core/src/meta/daily.ts:154` | `DailyRun` is exported but read only inside its own file (3) |
| certain | `packages/core/src/meta/daily.ts:179` | `dailyRunFor` is read only by tests (4) |
| certain | `packages/core/src/meta/features.ts:18` | `FeatureDef` is exported but read only inside its own file (1) |
| certain | `packages/core/src/meta/features.ts:42` | `FEATURES` is exported but read only inside its own file (4) |
| certain | `packages/core/src/meta/features.ts:121` | `defaultFeatures` is exported but read only inside its own file (1) |
| certain | `packages/core/src/meta/goals.ts:22` | `isGoalMet` is exported but read only inside its own file (1) |
| certain | `packages/core/src/meta/identity.ts:50` | `ICON_SVG` is exported but read only inside its own file (1) |
| certain | `packages/core/src/meta/mark.ts:91` | `MARK_GROUP` is exported but read only inside its own file (1) |
| certain | `packages/core/src/meta/progress.ts:42` | `Upgrade` is exported but read only inside its own file (4) |
| certain | `packages/core/src/meta/progress.ts:76` | `Perk` is exported but read only inside its own file (2) |
| certain | `packages/core/src/meta/progress.ts:250` | `slotsOf` is read only by tests (2) |
| certain | `packages/core/src/meta/records.ts:19` | `Records` is exported but read only inside its own file (5) |
| certain | `packages/core/src/meta/records.ts:33` | `EMPTY` is exported but read only inside its own file (2) |
| certain | `packages/core/src/meta/records.ts:143` | `gateD` is read only by tests (5) |
| certain | `packages/core/src/meta/report.ts:18` | `CRASH_DSN` is exported but read only inside its own file (1) |
| certain | `packages/core/src/meta/route.ts:20` | `Route` is exported but read only inside its own file (2) |
| certain | `packages/core/src/meta/route.ts:30` | `HOME` is read only by tests (4) |
| certain | `packages/core/src/meta/share.ts:37` | `Shared` is exported but read only inside its own file (1) |
| certain | `packages/core/src/meta/shedLadder.ts:61` | `ShedRung` is exported but read only inside its own file (1) |
| certain | `packages/core/src/meta/timeline.ts:39` | `HighlightKind` is exported but read only inside its own file (4) |
| certain | `packages/core/src/meta/timeline.ts:42` | `Highlight` is exported but read only inside its own file (5) |
| certain | `packages/core/src/meta/timeline.ts:52` | `RunDetail` is exported but read only inside its own file (7) |
| certain | `packages/core/src/meta/timeline.ts:100` | `DailyEntry` is exported but read only inside its own file (3) |
| certain | `packages/core/src/meta/timeline.ts:136` | `SharedEntry` is exported but read only inside its own file (5) |
| certain | `packages/core/src/meta/timeline.ts:152` | `WorldEventEntry` is exported but read only inside its own file (7) |
| certain | `packages/core/src/meta/timeline.ts:403` | `worldEventsOf` is read only by tests (6) |
| certain | `packages/core/src/meta/timeline.ts:423` | `sharedOf` is read only by tests (4) |
| certain | `packages/core/src/meta/timeline.ts:438` | `prehistory` is read only by tests (4) |
| certain | `packages/core/src/render/Renderer.ts:134` | `Renderer` is exported and nothing reads it, anywhere |
| certain | `packages/core/src/render/labels.ts:12` | `Label` is exported but read only inside its own file (1) |
| certain | `packages/core/src/render/layout.ts:73` | `hexAt` is read only by tests (6) |
| certain | `packages/core/src/render/layout.ts:110` | `zoomLayout` is read only by tests (4) |
| certain | `packages/core/src/render/materials.ts:22` | `CellSurface` is exported but read only inside its own file (2) |
| certain | `packages/core/src/render/paint.ts:30` | `Stop` is exported but read only inside its own file (3) |
| certain | `packages/core/src/render/paint.ts:109` | `REFERENCE_HEX_PX` is exported but read only inside its own file (1) |
| certain | `packages/core/src/render/paint.ts:116` | `patternScale` is exported but read only inside its own file (1) |
| certain | `packages/core/src/render/paint.ts:119` | `PaintOpts` is exported but read only inside its own file (1) |
| certain | `packages/core/src/render/paint.ts:228` | `patternTile` is exported but read only inside its own file (1) |
| certain | `packages/core/src/render/paint.ts:360` | `planKey` is read only by tests (2) |
| certain | `packages/core/src/render/paint.ts:375` | `SurfaceSamples` is exported but read only inside its own file (1) |
| certain | `packages/core/src/render/paint.ts:399` | `surfaceSamples` is read only by tests (7) |
| certain | `packages/core/src/sim/policy.ts:35` | `Move` is exported but read only inside its own file (12) |
| certain | `packages/core/src/sim/policy.ts:164` | `randomLegal` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/policy.ts:182` | `farm` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/policy.ts:201` | `rush` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/policy.ts:234` | `hoard` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/policy.ts:252` | `trickle` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/policy.ts:313` | `spender` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/policy.ts:344` | `bank3` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/policy.ts:345` | `bank15` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/policy.ts:347` | `bank20` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/policy.ts:348` | `bank40` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/policy.ts:350` | `bank80` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/policy.ts:410` | `seeker` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/policy.ts:443` | `chooser` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/policy.ts:507` | `survivor` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/policy.ts:526` | `blind` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/policy.ts:560` | `timid` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/policy.ts:579` | `greedy` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/policy.ts:597` | `tourist` is exported but read only inside its own file (1) |
| certain | `packages/core/src/sim/report.ts:12` | `Summary` is exported but read only inside its own file (3) |
| certain | `packages/core/src/sim/run.ts:18` | `Outcome` is exported but read only inside its own file (2) |
| certain | `packages/core/src/sim/run.ts:66` | `RunOptions` is exported but read only inside its own file (2) |
| certain | `packages/core/src/sim/run.ts:134` | `playRun` is exported but read only inside its own file (1) |
| certain | `packages/core/src/text/Strings.ts:23` | `LessonHead` is exported but read only inside its own file (22) |
| certain | `packages/core/src/text/Strings.ts:33` | `LessonStrings` is exported but read only inside its own file (2) |
| certain | `packages/core/src/theme/assets.ts:22` | `AssetSlot` is exported but read only inside its own file (1) |
| certain | `packages/core/src/theme/css.ts:15` | `CssVars` is exported but read only inside its own file (1) |
| certain | `packages/core/src/theme/icons.ts:251` | `CHROME_ICON` is read only by tests (3) |
| certain | `packages/core/src/theme/index.ts:113` | `DEFAULT_THEME_ID` is exported but read only inside its own file (3) |
| certain | `packages/core/src/theme/rig.ts:31` | `Vec3` is exported but read only inside its own file (9) |
| certain | `packages/core/src/theme/rig.ts:33` | `Light` is exported but read only inside its own file (1) |
| certain | `packages/core/src/theme/rig.ts:46` | `UP` is exported but read only inside its own file (3) |
| certain | `packages/core/src/theme/rig.ts:53` | `FLAT_RIG` is exported but read only inside its own file (2) |
| certain | `packages/core/src/theme/rig.ts:63` | `exposure` is exported but read only inside its own file (1) |
| certain | `packages/core/src/theme/rig.ts:68` | `normalised` is exported but read only inside its own file (1) |
| certain | `packages/core/src/theme/rig.ts:117` | `sideNormals` is read only by tests (5) |
| certain | `packages/core/src/theme/rig.ts:126` | `litColour` is exported but read only inside its own file (1) |
| certain | `packages/core/src/theme/rig.ts:138` | `renders` is read only by tests (10) |
| certain | `packages/core/src/theme/tokens.ts:91` | `luminance` is exported but read only inside its own file (3) |
| certain | `packages/core/src/theme/tokens.ts:115` | `contrastRatio` is exported but read only inside its own file (3) |
| certain | `packages/core/src/theme/tokens.ts:133` | `clearance` is exported but read only inside its own file (2) |
| certain | `packages/core/src/theme/tokens.ts:142` | `MIN_MARK_CONTRAST` is exported but read only inside its own file (2) |
| certain | `packages/core/src/theme/tokens.ts:171` | `edgeCasing` is read only by tests (3) |
| certain | `packages/core/src/theme/tokens.ts:268` | `GlyphShape` is exported but read only inside its own file (1) |
| certain | `packages/core/src/theme/tokens.ts:270` | `NO_PATTERN` is exported but read only inside its own file (3) |
| certain | `packages/core/src/theme/tokens.ts:346` | `Ink` is exported but read only inside its own file (1) |
| certain | `packages/core/src/theme/tokens.ts:432` | `Type` is exported but read only inside its own file (1) |
| certain | `packages/core/src/theme/tokens.ts:454` | `Board` is exported but read only inside its own file (1) |
| certain | `packages/core/src/theme/tokens.ts:758` | `Fog` is exported but read only inside its own file (1) |
| certain | `packages/core/src/theme/tokens.ts:816` | `MIN_FIELD_LIFT` is exported but read only inside its own file (2) |
| certain | `packages/core/src/theme/tokens.ts:837` | `MIN_LIT_FIELD_LIFT` is read only by tests (3) |
| certain | `packages/core/src/theme/tokens.ts:935` | `fieldDots` is exported but read only inside its own file (2) |
| certain | `packages/core/src/theme/tokens.ts:1046` | `fieldPattern` is exported but read only inside its own file (2) |
| certain | `packages/core/src/theme/tokens.ts:1077` | `fieldOverlayPattern` is exported but read only inside its own file (1) |
| certain | `packages/core/src/theme/tokens.ts:1117` | `FieldGround` is exported but read only inside its own file (1) |
| certain | `packages/core/src/theme/torch.ts:27` | `CellLight` is exported but read only inside its own file (1) |
| certain | `packages/core/src/theme/torch.ts:43` | `DIMMED_STEP` is exported but read only inside its own file (1) |
| certain | `packages/core/src/view/figure.ts:43` | `FigCell` is exported but read only inside its own file (2) |
| certain | `packages/core/src/view/figure.ts:67` | `FigCard` is exported but read only inside its own file (1) |
| certain | `packages/core/src/view/figure.ts:69` | `FigureSpec` is exported but read only inside its own file (2) |
| certain | `packages/core/src/view/figure.ts:169` | `FigureLayout` is exported but read only inside its own file (1) |
| certain | `packages/core/src/view/lessons.ts:100` | `Weight` is exported but read only inside its own file (3) |
| certain | `packages/core/src/view/lessons.ts:115` | `Beat` is exported but read only inside its own file (2) |
| certain | `packages/core/src/view/lessons.ts:192` | `lessonCore` is read only by tests (5) |
| certain | `packages/core/src/view/receipts.ts:72` | `Receipt` is exported but read only inside its own file (3) |
| certain | `packages/core/src/view/receipts.ts:86` | `ReceiptContext` is exported but read only inside its own file (3) |
| certain | `packages/core/src/view/tips.ts:23` | `ConceptTerm` is exported but read only inside its own file (3) |
| certain | `packages/core/src/view/view.ts:143` | `RenderContext` is exported but read only inside its own file (5) |
| certain | `packages/core/src/view/view.ts:1088` | `ColourPotential` is exported but read only inside its own file (3) |
| certain | `packages/core/src/view/view.ts:1191` | `runwayOf` is exported but read only inside its own file (1) |
| certain | `packages/core/src/view/view.ts:1301` | `whatGlows` is exported but read only inside its own file (1) |
| certain | `packages/core/src/view/view.ts:1351` | `epitaphFor` is exported but read only inside its own file (1) |
| certain | `packages/core/src/view/view.ts:1399` | `rarityLine` is exported but read only inside its own file (1) |
| certain | `packages/core/src/view/view.ts:1616` | `SetLesson` is exported but read only inside its own file (1) |
| certain | `packages/core/src/view/view.ts:1740` | `groundHead` is exported but read only inside its own file (1) |
| certain | `packages/core/src/view/view.ts:1761` | `powerHead` is exported but read only inside its own file (1) |
| certain | `packages/core/src/view/view.ts:1835` | `DescribeContext` is exported but read only inside its own file (1) |
| certain | `scripts/ladder.ts:49` | `Ladder` is exported but read only inside its own file (2) |
| certain | `scripts/ladder.ts:57` | `terrainFile` is exported but read only inside its own file (1) |
| certain | `scripts/sweep/allow.ts:20` | `Ruling` is exported but read only inside its own file (3) |
| certain | `scripts/sweep/program.ts:67` | `sourceFiles` is exported but read only inside its own file (1) |
| certain | `scripts/sweep/program.ts:82` | `options` is exported but read only inside its own file (1) |

## field — 70

| grade | where | what |
| --- | --- | --- |
| certain | `apps/game/src/board/rings.ts:20` | `Ring.width` is WRITTEN (8) and never read |
| certain | `apps/game/src/shell/cross.ts:79` | `Crossed.carried` is WRITTEN (1) and never read — though 4 test read(s) exist |
| certain | `apps/game/src/shell/keeper.ts:89` | `Keeper.alive` is WRITTEN (1) and never read — though 7 test read(s) exist |
| certain | `apps/game/src/shell/settle.ts:307` | `SettledDaily.isNewBest` is WRITTEN (2) and never read — though 2 test read(s) exist |
| certain | `apps/game/src/shell/store.ts:41` | `Snapshot.harvestAt` is WRITTEN (1) and never read |
| certain | `apps/game/src/shell/store.ts:165` | `Session.theme` is WRITTEN (1) and never read |
| certain | `apps/game/src/shell/store.ts:166` | `Session.strings` is WRITTEN (1) and never read |
| certain | `e2e/audit/audit.ts:19` | `Finding.kind` is WRITTEN (5) and never read — though 3 test read(s) exist |
| certain | `e2e/audit/audit.ts:21` | `Finding.where` is WRITTEN (5) and never read — though 1 test read(s) exist |
| certain | `e2e/audit/audit.ts:23` | `Finding.text` is WRITTEN (5) and never read — though 1 test read(s) exist |
| certain | `e2e/audit/audit.ts:24` | `Finding.detail` is WRITTEN (5) and never read — though 1 test read(s) exist |
| certain | `e2e/audit/audit.ts:26` | `Finding.value` is WRITTEN (5) and never read — though 3 test read(s) exist |
| certain | `e2e/audit/audit.ts:27` | `Finding.bar` is WRITTEN (5) and never read — though 3 test read(s) exist |
| certain | `packages/core/src/engine/rng.ts:85` | `RngStreams.region` is WRITTEN (1) and never read — though 1 test read(s) exist |
| certain | `packages/core/src/engine/state.ts:157` | `HarvestRecord.tiles` is WRITTEN (6) and never read |
| certain | `packages/core/src/engine/state.ts:208` | `GameState.version` is WRITTEN (1) and never read |
| certain | `packages/core/src/meta/backup.ts:115` | `Backup.format` is WRITTEN (3) and never read — though 1 test read(s) exist |
| certain | `packages/core/src/meta/backup.ts:117` | `Backup.sha` is WRITTEN (3) and never read — though 1 test read(s) exist |
| certain | `packages/core/src/meta/features.ts:19` | `FeatureDef.id` is WRITTEN (3) and never read |
| certain | `packages/core/src/meta/features.ts:20` | `FeatureDef.defaultOn` is WRITTEN (3) and never read |
| certain | `packages/core/src/meta/features.ts:26` | `FeatureDef.wired` is WRITTEN (3) and never read |
| certain | `packages/core/src/meta/features.ts:35` | `FeatureDef.player` is WRITTEN (3) and never read |
| certain | `packages/core/src/meta/route.ts:22` | `Route.seed` is WRITTEN (2) and never read — though 6 test read(s) exist |
| certain | `packages/core/src/meta/timeline.ts:42` | `Highlight.kind` is WRITTEN (10) and never read |
| certain | `packages/core/src/meta/timeline.ts:42` | `Highlight.n` is WRITTEN (3) and never read |
| certain | `packages/core/src/meta/timeline.ts:53` | `RunDetail.placements` is WRITTEN (7) and never read |
| certain | `packages/core/src/meta/timeline.ts:56` | `RunDetail.popped` is WRITTEN (7) and never read |
| certain | `packages/core/src/meta/timeline.ts:58` | `RunDetail.bigPop` is WRITTEN (7) and never read |
| certain | `packages/core/src/meta/timeline.ts:59` | `RunDetail.bigPopAt` is WRITTEN (7) and never read |
| certain | `packages/core/src/meta/timeline.ts:61` | `RunDetail.claims` is WRITTEN (7) and never read |
| certain | `packages/core/src/meta/timeline.ts:62` | `RunDetail.quests` is WRITTEN (7) and never read |
| certain | `packages/core/src/meta/timeline.ts:87` | `RunEntry.worldSeed` is WRITTEN (4) and never read |
| certain | `packages/core/src/meta/timeline.ts:92` | `RunEntry.highlights` is WRITTEN (6) and never read |
| certain | `packages/core/src/meta/timeline.ts:107` | `DailyEntry.arc` is WRITTEN (3) and never read |
| certain | `packages/core/src/meta/timeline.ts:111` | `DailyEntry.best` is WRITTEN (3) and never read |
| certain | `packages/core/src/meta/timeline.ts:158` | `WorldEventEntry.worldSeed` is WRITTEN (9) and never read — though 1 test read(s) exist |
| certain | `packages/core/src/meta/timeline.ts:160` | `WorldEventEntry.n` is WRITTEN (3) and never read — though 1 test read(s) exist |
| certain | `packages/core/src/render/Renderer.ts:99` | `CellView.light` is WRITTEN (9) and never read — though 7 test read(s) exist |
| certain | `packages/core/src/render/paint.ts:387` | `SurfaceSamples.label` is WRITTEN (1) and never read — though 6 test read(s) exist |
| certain | `packages/core/src/render/paint.ts:396` | `SurfaceSamples.face` is WRITTEN (1) and never read — though 5 test read(s) exist |
| certain | `packages/core/src/sim/policy.ts:40` | `Policy.note` is WRITTEN (14) and never read |
| certain | `packages/core/src/sim/run.ts:27` | `RunResult.policy` is WRITTEN (1) and never read |
| certain | `packages/core/src/sim/run.ts:28` | `RunResult.seed` is WRITTEN (1) and never read |
| certain | `packages/core/src/sim/run.ts:30` | `RunResult.death` is WRITTEN (1) and never read — though 1 test read(s) exist |
| certain | `packages/core/src/sim/run.ts:59` | `RunResult.bestHarvest` is WRITTEN (1) and never read |
| certain | `packages/core/src/sim/run.ts:63` | `RunResult.steps` is WRITTEN (1) and never read |
| certain | `packages/core/src/text/Strings.ts:168` | `Strings.typography` is WRITTEN (2) and never read — though 1 test read(s) exist |
| certain | `packages/core/src/text/Strings.ts:175` | `Strings.luckCore` is WRITTEN (2) and never read — though 1 test read(s) exist |
| certain | `packages/core/src/theme/assets.ts:24` | `AssetSlot.label` is WRITTEN (13) and never read |
| certain | `packages/core/src/theme/assets.ts:28` | `AssetSlot.note` is WRITTEN (13) and never read |
| certain | `packages/core/src/theme/assets.ts:30` | `AssetSlot.wired` is WRITTEN (13) and never read |
| certain | `packages/core/src/theme/assets.ts:32` | `AssetSlot.tiling` is WRITTEN (13) and never read |
| certain | `packages/core/src/theme/tokens.ts:651` | `Theme.source` is WRITTEN (2) and never read |
| certain | `packages/core/src/theme/tokens.ts:760` | `Fog.alpha` is WRITTEN (2) and never read — though 1 test read(s) exist |
| certain | `packages/core/src/view/figure.ts:174` | `FigureLayout.halfW` is WRITTEN (2) and never read |
| certain | `packages/core/src/view/figure.ts:175` | `FigureLayout.halfH` is WRITTEN (2) and never read |
| certain | `packages/core/src/view/view.ts:669` | `HudView.tilesSpare` is WRITTEN (1) and never read |
| certain | `packages/core/src/view/view.ts:732` | `HudView.colours` is WRITTEN (1) and never read |
| certain | `packages/core/src/view/view.ts:734` | `HudView.spotlight` is WRITTEN (1) and never read |
| certain | `packages/core/src/view/view.ts:741` | `HudView.pocketsReady` is WRITTEN (1) and never read — though 2 test read(s) exist |
| certain | `packages/core/src/view/view.ts:760` | `HudView.questPays` is WRITTEN (1) and never read |
| certain | `packages/core/src/view/view.ts:784` | `HudView.guide` is WRITTEN (1) and never read — though 1 test read(s) exist |
| certain | `packages/core/src/view/view.ts:1091` | `ColourPotential.count` is declared and never mentioned again |
| certain | `packages/core/src/view/view.ts:1093` | `ColourPotential.worth` is declared and never mentioned again |
| certain | `packages/core/src/view/view.ts:1101` | `ColourPotential.bonus` is declared and never mentioned again |
| certain | `packages/core/src/view/view.ts:1102` | `ColourPotential.ripeCount` is declared and never mentioned again |
| certain | `packages/core/src/view/view.ts:1104` | `ColourPotential.ripeWorth` is declared and never mentioned again |
| certain | `scripts/sweep/allow.ts:23` | `Ruling.on` is declared and never mentioned again |
| certain | `scripts/sweep/finding.ts:33` | `Finding.detail` is WRITTEN (3) and never read |
| certain | `scripts/sweep/program.ts:111` | `Workspace.checker` is WRITTEN (1) and never read |
