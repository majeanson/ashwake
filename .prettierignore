node_modules
dist
pnpm-lock.yaml
**/__snapshots__
# Generated by `pnpm audit:screens`, and the reason it is ignored rather than
# formatted: the audit writes it from a template on every run, so any run that
# is not followed by a `pnpm format` lands an unformatted file on `main` and
# CI's `format:check` fails on a report nobody edited. It cost this session a
# red build on 2026-08-30. A generated artefact answers to its generator.
audit-shots/report.md

# Generated by `pnpm sweep`, ignored for exactly the reason above it: a report
# rewritten from a template on every run, formatted by nothing but its own
# generator. `PASS.md` P1 is what it is for.
SWEEP.md
